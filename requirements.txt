# Paper Curation — full dependency set.
#
# 표준 환경은 conda env `py312` (Python 3.14 의 numba CALL_KW 비호환을 피한 단일
# env) 입니다. 모든 단계 — 오케스트레이터·LLM·웹·PDF·HTML 은 물론 토픽 모델링/분류
# (topic_modeling.py · classify_papers.py) 까지 이 한 env 에서 돌아갑니다.
# umap-learn / hdbscan / sentence-transformers 는 macOS 에서 pip 보다 conda-forge
# 가 안정적이므로, README "환경 설정" 의 conda 명령으로 env 를 만든 뒤
# `pip install -r requirements.txt` 로 나머지를 채우는 것을 권장합니다.
#
# NOTE: py312 **단독** 입니다. 과거의 "py314 메인 + py312 보조" 이중 구성은
# 더 이상 지원하지 않습니다 — 모든 진입점이 `_env_guard.force_py312()` 로
# py312 가 아니면 py312 로 재실행하고, 못 찾으면 명확히 실패합니다
# (py314 로는 절대 진행하지 않음). py312 가 표준 위치에 없으면
# `PAPER_CURATION_PY312` 로 절대 경로를 지정하세요.

# --- Core: LLM / web / PDF / image ---
anthropic
openai
google-genai
pymupdf
Pillow
requests
pyzotero
opendataloader-pdf

# --- Metrics (run_metrics.py: citations.md / references.md front-matter) ---
# lib/metrics/store.py 가 최상단에서 import 한다. 없으면 step 1.5 가 매 사이클
# 조용히 실패한다 (soft step 이라 파이프라인은 계속 돈다).
PyYAML

# --- Topic modeling / classification (also required in the py312 env) ---
numpy
scikit-learn
joblib
umap-learn
hdbscan
sentence-transformers

# --- Citedby (인용논문 분석: pipeline/lib/citedby/) ---
# 지연 import 라 citedby 를 쓰지 않으면 로드되지 않는다.
# NOTE: Scopus 는 pybliometrics **패키지**가 아니라 그 설정 파일
#       (~/.config/pybliometrics.cfg) 의 API 키만 읽고 requests 로 호출하므로
#       pybliometrics 자체는 의존성이 아니다.
pandas
arxiv
