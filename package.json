{
  "name": "paper-curation",
  "version": "0.0.0",
  "private": false,
  "description": "Dependency-free NPX CLI for paper-curation setup and local workflow planning.",
  "type": "module",
  "bin": {
    "paper-curation": "./bin/paper-curation.mjs"
  },
  "files": [
    "bin",
    "pipeline",
    "pyproject.toml",
    "requirements.txt",
    "src",
    "paper-curation.yaml"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test:npx-cli": "node --test tests/npx-cli.test.mjs"
  },
  "dependencies": {}
}
